# Bind all interfaces inside the container; Spark master is the standalone cluster.
livy.server.host = 0.0.0.0
livy.server.port = 8998

livy.spark.master = spark://spark-master:7077

# Allow Livy to load user JARs from file:// (local smoke tests under /data).
livy.repl.enable-hive-context = false
